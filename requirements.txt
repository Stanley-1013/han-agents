# HAN-Agents Dependencies
# Core: no external dependencies (stdlib only)
#
# Optional: Enhanced code extraction via Tree-sitter AST parsing
# Install with: pip install -r requirements-ast.txt
# Or auto-installed on first use
